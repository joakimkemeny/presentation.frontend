<nav {{bindAttr class=":l-sideForm controllers.appointments.showEditAppointment:l-visible:l-hidden"}}>
	<h3>Bokning</h3>

	<div class="l-horizontal">
		<div class="form-group f-floating-label-form-group">
			{{input value=date class="form-control" placeholder="Datum"}}
		</div>
	</div>
	<div class="l-horizontal">
		<div class="form-group f-floating-label-form-group">
			{{input value=start class="form-control" placeholder="Starttid"}}
		</div>
		<div class="form-group f-floating-label-form-group">
			{{input value=end class="form-control" placeholder="Sluttid"}}
		</div>
	</div>
	<div class="l-horizontal">
		<div class="form-group f-floating-label-form-group">
			{{textarea value=notes class="form-control" placeholder="Anteckningar"}}
		</div>
	</div>

	<div class="f-actions">
		<button class="btn btn-success" {{action "save"}}><i class="fa fa-check"></i> Spara</button>
		<button class="btn" {{action "cancel"}}>Avbryt</button>
	</div>
</nav>
